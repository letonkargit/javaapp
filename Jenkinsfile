
pipeline{
    agent any
    stages{
        stage('build') {
            steps{
                echo "starting build!"
                sh 'make'
                echo "build complete!"
            }
        }
    }
}